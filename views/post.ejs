<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href=".\bootstrap-5.2.0-beta1-dist\css\bootstrap.min.css">
    <title><%= post.title %> | Blog App </title>
</head>
<body class="bg-dark">
    <nav class="navbar bg-dark navbar-dark pt-4 mb-5">
        <div class="container">
          <a href="/" class="navbar-brand">Blog App</a>
          <% if(isAuthenticated) { %>
            <div class="d-flex">
              <a href="/newpost" class="btn btn-primary me-2">Create a New Post</a>
              <a class="btn btn-success me-2" href="/profile"><%= userProfile.nickname %></a>
              <a class="btn btn-warning me-2" href="/logout">Logout</a>
            </div>
            <% } else{ %>
          <div class="d-flex">
            <a class="btn btn-success me-2" href="/login">Signup or Login</a>
          </div>
          <% } %>
        </div>
    </nav>
    
    <div class="container text-light">
        <h2><%= post.title %></h2>
        <p>by <%= post.authorName %></p>
        <p class="card-text"><%= post.content %></p>
    </div>
    <div class="container fixed-bottom bg-secondary text-center text-light py-1">
      <span>Developed by <a class="text-light" href="https://github.com/temalcode" target="_blank">@temalcode</a>.</span>
  </div>
</body>
</html>